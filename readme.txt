The `code.R` can be run with R 4.0.0+ with latest version of tidyverse, survival, cmprsk, xlsx installed. The `dummy_df.rds` is the dummy data as a supplement to the code, generated by the synthpop package for test purpose.